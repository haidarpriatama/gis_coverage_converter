ENVIRONMENT=development
FRONTEND_ORIGINS=http://localhost:3000
# Optional, production only: comma-separated backend hostnames without scheme.
TRUSTED_HOSTS=localhost,127.0.0.1,testserver
MAX_UPLOAD_SIZE_MB=1024
